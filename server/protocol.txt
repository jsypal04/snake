1. Tells the server/client if this contains state data
    GET - fetch the state from the server
    PUSH - push the state to the server
2. Authentication token - this line authenticates the client as an authentic snake client
3. Payload - JSON encoded data

status codes:
    200 - OK
    400 - Generic Error
    401 - Invalid Command

